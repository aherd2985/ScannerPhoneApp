<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Todo.TodoItemPage" Title="{Binding Name}">
    <StackLayout Margin="20" VerticalOptions="StartAndExpand">
        <Label Text="Name" />
        <Entry Text="{Binding Name}" />
        <Label Text="Type" />
        <Entry Text="{Binding CodeType}" />
        <Label Text="Code" />
        <Entry Text="{Binding Code}" />
        <Label Text="Sync" />
        <Switch IsToggled="{Binding Sync}" />
        <Button Text="Save" Clicked="OnSaveClicked" />
        <Button Text="Delete" Clicked="OnDeleteClicked" />
        <Button Text="Cancel" Clicked="OnCancelClicked" />
        <Button Text="Preview" Clicked="OnPreviewClicked" />
    </StackLayout>
</ContentPage>